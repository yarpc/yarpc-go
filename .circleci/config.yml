version: 2
jobs:
  build:
    docker:
      - image: docker:17.03.1-ce
        environment:
          - SUPPRESS_CROSSDOCK: 1
          - CI_TYPES: lint test examples
    working_directory: /app
    steps:
      - checkout
      - setup_remote_docker
      - run: make ci
