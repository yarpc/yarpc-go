// Code generated by thriftrw-plugin-yarpc
// @generated

package bazserver

import (
	"context"
	"go.uber.org/thriftrw/wire"
	"go.uber.org/yarpc/api/transport"
	"go.uber.org/yarpc/encoding/thrift"
	"go.uber.org/yarpc/encoding/thrift/thriftrw-plugin-yarpc/internal/tests/extends"
	"go.uber.org/yarpc/encoding/thrift/thriftrw-plugin-yarpc/internal/tests/extends/barserver"
)

// Interface is the server-side interface for the Baz service.
type Interface interface {
	barserver.Interface

	Baz(
		ctx context.Context,
	) error
}

// New prepares an implementation of the Baz service for
// registration.
//
// 	handler := BazHandler{}
// 	dispatcher.Register(bazserver.New(handler))
func New(impl Interface, opts ...thrift.RegisterOption) []transport.Procedure {
	h := handler{impl}
	service := thrift.Service{
		Name: "Baz",
		Methods: []thrift.Method{

			thrift.Method{
				Name: "baz",
				HandlerSpec: thrift.HandlerSpec{

					Type:  transport.Unary,
					Unary: thrift.UnaryHandler(h.Baz),
				},
				Signature:    "Baz()",
				ThriftModule: extends.ThriftModule,
			},
		},
	}

	procedures := make([]transport.Procedure, 0, 1)
	procedures = append(procedures, barserver.New(impl, opts...)...)
	procedures = append(procedures, thrift.BuildProcedures(service, opts...)...)
	return procedures
}

type handler struct{ impl Interface }

func (h handler) Baz(ctx context.Context, body wire.Value) (thrift.Response, error) {
	var args extends.Baz_Baz_Args
	if err := args.FromWire(body); err != nil {
		return thrift.Response{}, err
	}

	err := h.impl.Baz(ctx)

	hadError := err != nil
	result, err := extends.Baz_Baz_Helper.WrapResponse(err)

	var response thrift.Response
	if err == nil {
		response.IsApplicationError = hadError
		response.Body = result
	}
	return response, err
}
